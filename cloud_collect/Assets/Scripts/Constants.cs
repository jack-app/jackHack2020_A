using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class Constants
{
    public readonly static Rect camera_view = new Rect(-9, -5, 18, 10);
    public readonly static float world_x_min = 0.5f;
    public readonly static float world_x_max = 0.75f;
    public readonly static float cloud_x_max = 0.25f;
    public readonly static float cloud_y_max = 0.15f;
    public readonly static float pop_min = 0.2f / 60;
    public readonly static float pop_max = 0.4f / 60;
}
